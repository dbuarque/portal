<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>LuPoEx</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <base href="<%- htmlWebpackPlugin.options.metadata.baseUrl %>">

    <!--<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">-->
    <!--<script src="https://cdnjs.cloudflare.com/ajax/libs/stellar-sdk/0.7.3/stellar-sdk.min.js"></script>-->
    <script src="/stellar-sdk.min.js"></script>

    <style>
        .splash {
            text-align: center;
            margin: 10% 0 0 0;
            box-sizing: border-box;
        }

        .splash .message {
            font-size: 30px;
            color: #5b6a72;
            font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
        }

        .splash .fa-refresh {
            text-align: center;
            display: inline-block;
            margin-top: 5px;
            color: #08b5e5;
        }
    </style>

    <!-- imported CSS are concatenated and added automatically -->
    <script>
        lupoex = {
            env: '<%- htmlWebpackPlugin.options.metadata.production ? 'production' : 'development' %>'
        };
    </script>
    <% } %>
</head>
<body aurelia-app="main">
<div class="splash">
    <div class="message">LuPoEx</div>
    <i class="fa fa-refresh fa-spin fa-2x"></i>
</div>
<% if (htmlWebpackPlugin.options.metadata.server) { %>
<!-- Webpack Dev Server reload -->
<script src="/webpack-dev-server.js"></script>
<% } %>
</body>
</html>
